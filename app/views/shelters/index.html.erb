<%= link_to "Shelter Index", "/shelters" %>
</br>
<%= link_to "Pet Index", "/pets" %>
</br>
<h1>Welcome to Adopt Don't Shop</h1>

<h2>Shelters:</h2>

<% @shelters.each do |shelter| %>
  <p><%= link_to "#{shelter.name}", "/shelters/#{shelter.id}/pets" %> <%= link_to "Edit", "/shelters/#{shelter.id}/edit" %> <%= link_to "Delete", "/shelters/#{shelter.id}", method: :delete %> </p>
<% end %>

<%= link_to "New Shelter", "/shelters/new" %>


