
<h2>Shelters:</h2>
<hr/>

<% @shelters.each do |shelter| %>
  <p><%= link_to "#{shelter.name}", "/shelters/#{shelter.id}" %> <%= link_to "Edit", "/shelters/#{shelter.id}/edit" %> <%= link_to "Delete", "/shelters/#{shelter.id}", method: :delete %> </p>
<% end %>
<hr/>

 <p><%= link_to "New Shelter", "/shelters/new" %></p>
