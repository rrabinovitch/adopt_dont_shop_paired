<h2>Favorited Pets</h2>

<% @pets.each do |pet| %>

<%  if @favorites.map(&:to_i).include?(pet.id) %>
<h1>   Name: <%= p pet.name %> </h1>
<%=    image_tag(pet.image, alt: "pet picture", method: :get, class: 'pet-image') %>
<%  end %>

<% end %>

  

